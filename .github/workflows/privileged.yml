name: privileged
on: workflow_dispatch

jobs:
  privileged:
    runs-on: ubuntu-latest
    container:
      image: concourse/concourse
      options: --privileged

    steps:
      - name: simple-task
        with:
          entrypoint: /bin/echo
          args: Hello, world!
